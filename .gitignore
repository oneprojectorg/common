# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files. `pnpm env:pull` writes .env.local / .env.<environment> from
# the templates in env/, so everything here is generated and holds real
# credentials. The .env.docker pair is the exception: committed local-dev
# defaults with no secrets in them.
.env
.env.*
!.env.docker
!.env.docker.example

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist
storybook-static
tsconfig.tsbuildinfo
next-env.d.ts
# Supabase
/supabase-old/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
.trees
.claude/worktrees

# Ralph
scripts/prd.json
scripts/progress.txt

# Python
__pycache__

# Yarn 4 files
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.aider*

specs/

# Various tools
.beads

# Playwright
tests/e2e/playwright-report/
tests/e2e/test-results/
tests/e2e/playwright/.auth/
tests/e2e/a11y-baseline/report.md
tests/e2e/a11y-baseline/report.json
tests/e2e/a11y-baseline/diff.md
tests/e2e/a11y-baseline/screenshots/
.playwright-mcp
ralph.sh
.fallow/

# pnpm local store (created inside bind-mounted /app when container runs pnpm)
.pnpm-store/

# Workspace-local one-shot/dev scripts (e.g. services/db/scripts/local/*.ts)
**/scripts/local/

# Local-only PRDs and verification artifacts
tasks/
.gstack/
.plans/
