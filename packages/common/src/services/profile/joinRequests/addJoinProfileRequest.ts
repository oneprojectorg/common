/**
 * Creates a new request from one profile to join another profile.
 */
export const addJoinProfileRequest = async (_params: {
  requestProfileId: string;
  targetProfileId: string;
}): Promise<void> => {
  // TODO: Implement
};
