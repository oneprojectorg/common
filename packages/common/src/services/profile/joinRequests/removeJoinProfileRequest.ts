/**
 * Removes an existing join profile request entirely (undo).
 */
export const removeJoinProfileRequest = async (_params: {
  requestProfileId: string;
  targetProfileId: string;
}): Promise<void> => {
  // TODO: Implement
};
