import { Heading } from '@react-email/components';

export const Header = (props: any) => (
  <Heading
    className="!my-0 mx-0 p-0 font-light text-left font-serif text-[28px] tracking-[-0.02625rem] text-[#222D38]"
    {...props}
  />
);
