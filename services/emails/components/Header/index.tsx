import { Heading } from '@react-email/components';

export const Header = (props: any) => (
  <Heading
    className="mx-0 !my-0 p-0 text-left font-serif text-title-lg text-neutral-black"
    {...props}
  />
);
