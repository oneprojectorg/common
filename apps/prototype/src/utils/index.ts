/**
 * PROTOTYPE ONLY.
 *
 * The one helper the copied components reach for. There is no asset host behind
 * a single-file prototype, so a key resolves to nothing and the avatar falls
 * back to its initials — which is what the seeded data wants anyway.
 */
export const getPublicUrl = (_key?: string | null) => undefined;
