# The root .gitignore excludes every `dist`. This app is the exception: the
# built page is committed so anyone can grab one file and drop it on a static
# host without cloning the monorepo or installing anything.
#
# A parent-excluded directory has to be re-included before anything inside it
# can be, hence all three lines.
!dist/
dist/*
!dist/index.html
