
import MainLayout from '@/app/(main)/layout';
import MainPage from '@/app/(main)/page';

const AppPage = () => {
  return (
    <MainLayout>
      <MainPage />
    </MainLayout>
  );
};

export default AppPage;
