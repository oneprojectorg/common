/**
 * Explicit null aside for the /edit route so soft navigations clear the aside
 * slot instead of preserving the previous active panel.
 */
export default function EditorAsideEditPage() {
  return null;
}
