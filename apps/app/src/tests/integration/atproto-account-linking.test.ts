import { describe, expect, it } from 'vitest';

describe('Integration: Account linking (Google + Bluesky)', () => {
  it('should link Bluesky to existing Google account by email', async () => {
    expect(true).toBe(false);
  });

  it('should allow login with either auth method after linking', async () => {
    expect(true).toBe(false);
  });

  it('should not create duplicate user accounts', async () => {
    expect(true).toBe(false);
  });
});
