import { describe, expect, it } from 'vitest';

describe('Integration: Email collection flow', () => {
  it('should show email collection modal when email missing', async () => {
    expect(true).toBe(false);
  });

  it('should create account after email submitted', async () => {
    expect(true).toBe(false);
  });

  it('should cleanup partial session after completion', async () => {
    expect(true).toBe(false);
  });
});
