import { describe, expect, it } from 'vitest';

describe('Integration: Allow list validation', () => {
  it('should reject user not on allow list', async () => {
    expect(true).toBe(false);
  });

  it('should show invite-only error message', async () => {
    expect(true).toBe(false);
  });

  it('should not create account for blocked users', async () => {
    expect(true).toBe(false);
  });
});
