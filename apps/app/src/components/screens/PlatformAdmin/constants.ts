/**
 * Shared grid layout for the users table
 * Columns: Name | Email | Role | Organization | Joined | Actions
 * Minimum width: ~850px (120+180+100+200+80+80 + gaps)
 */
export const USER_TABLE_GRID_COLS =
  'grid-cols-[minmax(120px,1fr)_minmax(180px,1.5fr)_minmax(100px,0.8fr)_minmax(200px,2.2fr)_minmax(80px,0.5fr)_80px]';

/**
 * Minimum width for the table to maintain proper layout
 */
export const USER_TABLE_MIN_WIDTH = 'min-w-[850px]';
