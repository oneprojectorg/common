import type { SectionProps } from '../../contentRegistry';

export default function SummarySectionContent(_props: SectionProps) {
  return (
    <div className="mx-auto w-full space-y-4 p-4 md:max-w-160 md:p-8">
      <h1>Summary</h1>
    </div>
  );
}
