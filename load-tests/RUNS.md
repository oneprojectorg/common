# Load test run log

One row per run so reports can be told apart later. Report files live alongside
this file as `report-<label>-<timestamp>.html` (gitignored). "Cap" = Supabase
pooler **Max client connections** (fixed by compute tier: Micro=200, Small=400).

| Date (2026) | Label / report stamp | Target | DB tier / cap | Profile | Headline result |
| --- | --- | --- | --- | --- | --- |
| 06-22 11:57 | `300vu` / `report-300vu-20260622-115755.html` | staging `app-dev`/`api-dev`, slug `staging-commonville-pb` | Micro / 200 | 300 VU | Prior session, **pre rate-limit-bypass** — limiter-bound (429s), not capacity |
| 06-22 12:58 | `300vu` / `report-300vu-20260622-125817.html` | staging `app-dev`/`api-dev`, slug `staging-commonville-pb` | Micro / 200 | 300 VU | Rate limit bypassed (E2E). **`http_req_failed` 6.33% — EMAXCONN (200 client-conn cap)**. listProposals p95 3.35s, page p95 6.7s |
| 06-22 13:13 | `300vu-400cap` / `report-300vu-400cap-20260622-131358.html` | staging `app-dev`/`api-dev`, slug `staging-commonville-pb` | **Small / 400** (matches prod) | 300 VU | EMAXCONN gone (**0.02%** fail), but bottleneck moved to 40-conn backend pool: page p95 **10.2s**, throughput ↓ (109 req/s). Latency-bound, not error-bound |
| 06-22 13:22 | `columbus-tiptap-300vu` / `report-columbus-tiptap-300vu-20260622-132247.html` | **preview** `app-git-dev-oneproject.vercel.app` (same-origin API), slug `decision-city-of-columbus-part` | Small / 400 + **TipTap cloud** for doc content | 300 VU | **0% errors**, but content path dominates: **listProposals p95 8.44s (med 7.06s)**, getProposal p95 4.2s (med 3.55s) — ~2× the DB-only process. throughput ↓ (84 req/s). Bottleneck = TipTap-backed content fetch, not DB conns |

| 06-22 13:37 | `columbus-tiptap-spike1000` / `report-columbus-tiptap-spike1000-20260622-133712.html` | **preview** `app-git-dev-oneproject.vercel.app`, slug `decision-city-of-columbus-part` | Small / 400 + TipTap | **Spike** 15s→**1000 VU**, hold 2m | **BROKE: `http_req_failed` 16.74%**. page p95 **36s**, iteration p95 **1m38s**. 3 failure modes (Axiom): EMAXCONN ×3,943 (400 cap bound under surge), `listProposalSubmitters` SSR relative-URL `ERR_INVALID_URL` ×~4.8k (preview-specific bug), function crash `exit 128` ×434 |
| 06-23 09:30 | `no-prefetch-300vu` / `report-no-prefetch-300vu-20260623-093026.html` | staging `app-dev`/`api-dev`, slug `staging-commonville-pb` | (tier not re-checked this run) | 300 VU | **After removing some prefetches.** `http_req_failed` **0.07%**, 0% rate-limited. Throughput **122.7 req/s** (↑ from 109 in the 06-22 13:13 Small/400 run), **page-process p95 8.72s** (↓ from 10.2s), page-proposal p95 7.22s. listProposals still slowest API (p95 3.7s, med 1.5s), getDecisionBySlug/getInstance p95 ~1.56s. All latency thresholds still crossed but improved; no error-mode regression |
| 06-23 09:38 | `no-prefetch-spike1000` / `report-no-prefetch-spike1000-20260623-093836.html` | staging `app-dev`/`api-dev`, slug `staging-commonville-pb` | (tier not re-checked this run) | **Spike** 15s→**1000 VU**, hold 2m | **BROKE: `http_req_failed` 8.39%** (vs 16.74% on the 06-22 preview spike — better, but still broken). page-process p95 **18.8s**, page-proposal p95 19.2s, iteration p95 **2m8s**. Failure mode is almost entirely **`dial: i/o timeout`** (~2.1k, can't even open the connection) split across app (736) + API getDecisionBySlug (664) / getInstance (433) / getCategories (207); plus ~50 request-timeouts and a handful of connection resets. **0% rate-limited, NO EMAXCONN** this run → bottleneck is edge/app connection-accept under surge, not the DB pooler. 1000 VU is still over capacity |
| 06-23 09:46 | `no-prefetch-spike1000-r2` / `report-no-prefetch-spike1000-r2-20260623-094624.html` | staging `app-dev`/`api-dev`, slug `staging-commonville-pb` | (tier not re-checked this run) | **Spike** 15s→**1000 VU**, hold 2m | **REPRO of 09:38 — not a transient client blip.** `http_req_failed` **9.42%**. Same failure mix: `dial: i/o timeout` ×**2032**, connection reset by peer ×136, request timeout ×97. 0% rate-limited, no EMAXCONN. page-process p95 **25.96s**, page-proposal p95 24.5s, iteration p95 2m6s. Zero warnings during the 15s ramp; all failures begin once held at 1000 VU. Caveat: `dial: i/o timeout` is a client-side connect failure — can't tell from k6 alone whether it's the server edge dropping SYNs or the single load-gen Mac saturating (ephemeral ports / ulimit). Needs Axiom server-side view to disambiguate |
| 06-23 17:43 | `submitter-cache-spike1000` / `report-submitter-cache-spike1000-20260623-174351.html` | staging `app-dev`/`api-dev` (with pages), slug `staging-commonville-pb` | + **submitter caching** (fewer DB hits on SSR submitter fetch) | **Spike** 15s→**1000 VU**, hold 2m | **SSR page render hugely improved; API path shifted slower.** vs 09:46 pre-cache r2: **page-process p95 25.96s → 1.73s, page-proposal 24.5s → 1.76s** (both now PASS the 3s threshold — caching cut the SSR submitter DB hits). BUT client-side tRPC got slower: **listProposals p95 7.12s → 19.26s, getProposal 1.63s → 17.52s** — because fast pages let iterations cycle faster (throughput 139 → **171 req/s**), pushing more concurrent load onto the uncached API/DB path. Hard failures ~unchanged: `http_req_failed` 10.24% (was 9.42%), **2120 dial: i/o timeout** (same edge ceiling, cache-independent), 0% rate-limited, no EMAXCONN. Overall query max 60s → **33.73s**. Net: caching fixed the page-render bottleneck; the API read path (listProposals/getProposal) is now the dominant latency under spike |
| 06-23 16:06 | `pr1335-rebased-api-spike1000` / `report-pr1335-rebased-api-spike1000-20260623-160555.html` | **PR #1335 preview (rebased)** same-origin API, slug `staging-commonville-pb`, `SKIP_PAGES=true` | PR defaults: DB_POOL_MAX=10, statement_timeout 30s, idle_in_txn 60s | **Spike** 15s→**1000 VU**, hold 2m, API-only | **Strong result for the PR.** `rate_limited` 0%, `http_req_failed` **2.06%** — and **NO EMAXCONN** (vs the 06-22 preview spike that broke with EMAXCONN ×3943). All 585 hard failures are **`dial: i/o timeout`** concentrated on getDecisionBySlug (first request per iteration) = the edge/connection-accept ceiling, NOT the DB. DB itself healthy: getCategories 0 fails, listProposals 58/6187 fail. **statement_timeout signature: overall query max = 25.08s, nothing crossed 30s** — vs pre-PR spikes where single queries pinned at 1m0s. Suggestive the 30s cap is bounding the tail (not definitive — didn't catch an explicit 30s cancellation). listProposals p95 19.77s / getProposal p95 16.54s still slow (content path). NB API-only, so 2.06% NOT comparable to the 8–12% page-based commonville spikes (different traffic shape) |
| 06-23 15:55 | `pr1335-rebased-api300vu` / `report-pr1335-rebased-api300vu-20260623-155553.html` | **PR #1335 preview (rebased onto dev → rate-limit bypass now present)** same-origin API, slug `staging-commonville-pb`, `SKIP_PAGES=true` | PR defaults: DB_POOL_MAX=10, statement_timeout 30s, idle_in_txn 60s | 300 VU API-only | **Clean — limiter bypassed (`rate_limited` 0.00%)**, real DB layer exercised. `http_req_failed` **1.31%** (654 fast-returning non-200s; no 429s, no dial/conn timeouts — likely transient 5xx). DB comfortable: listProposals p95 **1.32s** (med 0.88s), all other endpoints p95 <0.9s, overall **max 5.69s**, ~175 req/s. **statement_timeout (30s) never fired** — nothing ran long enough at this load, so its through-Supavisor behavior is still unverified. NB: API-only, so lighter than a full-SSR baseline (not comparable to 06-23 app-dev 300VU-with-pages where listProposals p95 was 3.7s). To actually engage the PR's pool/timeout protections, need higher load (spike) or include SSR pages |
| 06-23 12:52 | `pr1335-defaults-api300vu` / `report-pr1335-defaults-api300vu-20260623-125214.html` | **PR #1335 preview** `app-git-issue-one-40-db-pool-timeouts...vercel.app` (same-origin API), slug `staging-commonville-pb`, `SKIP_PAGES=true` | PR defaults: DB_POOL_MAX=10, statement_timeout 30s, idle_in_txn 60s | 300 VU API-only | **INCONCLUSIVE for the PR — limiter-bound, not capacity.** `rate_limited` **26.79%** / `http_req_failed` 28%. Per-endpoint 429s track the *configured* limits (getCategories limit-10 ~46% throttled, getDecisionBySlug limit-20 ~10%, getInstance limit-30 ~1%) = classic **single shared-bucket** signature → Vercel edge overwrites the per-VU `X-Forwarded-For` spoof, collapsing all 300 VUs into one rate-limit bucket. DB never stressed: all p95 <1.4s, **max 3.37s** (nothing approached the 30s statement_timeout, so its effect is unobservable here). Earlier preview/staging load runs bypassed the limiter via `E2E`; **this preview lacks that env**. Also: anon page GET 307→/login (no public page access). To test the PR, the preview needs the rate-limit bypass (E2E) set |
| 06-23 09:52 | `columbus-spike1000` / `report-columbus-spike1000-20260623-095206.html` | staging `app-dev`/`api-dev`, slug **`decision-city-of-columbus-part`** (fewer proposals) | (tier not re-checked this run) | **Spike** 15s→**1000 VU**, hold 2m | **Same connection ceiling, WORSE content latency.** `http_req_failed` **11.82%** (vs 9.42% commonville). Identical failure mix: `dial: i/o timeout` ×**1979**, conn reset ×45, request timeout ×17 — confirms the dial-timeout wall is process-independent (≈2k both runs), i.e. a fixed connect-side ceiling, not proposal count. But **`listProposals` p95 16.99s (med 7.74s)** and **getProposal p95 6.21s (med 3.67s)** — ~2× commonville — so this process's *content* path is heavier per-proposal despite fewer of them (TipTap-backed content, matches 06-22 columbus finding). Throughput ↓ to 108 req/s, iteration p95 2m15s. Fewer proposals ≠ faster: per-proposal content cost dominates |
| 06-24 15:43 | `issue855-spike1000` / `report-issue855-spike1000-20260624-154317.html` | **preview** `app-git-issue-1215898069326855-oneproject.vercel.app` (same-origin API), slug `staging-commonville-pb-f2f67c2d`, **with pages** | (tier not checked) | **Spike** 15s→**1000 VU**, hold 2m | **WORST page-based 1000 spike so far: `http_req_failed` 19.84%.** 0% rate_limited (per-VU IP spoof held — limiter not collapsed) and 0% page_redirected (anon public access works). Failures spread across ALL endpoints: getDecisionBySlug 71% pass, listProposals 60%, getProposal 65%, page-process 75%. **SSR pages uncached + brutal: page-process p95 52.49s, page-proposal p95 54.4s** (cf. submitter-cache run's 1.7s — that caching is NOT on this branch). API path also slow: listProposals p95 13.42s, getInstance p95 12.58s, getProposal p95 7.22s. **Multiple endpoints max out at exactly 1m0s** = 60s timeout wall (function/gateway), not the fast-fail `dial: i/o timeout` signature of the staging edge-ceiling spikes. Throughput 100.7 req/s, iteration p95 1m59s (max 2m53s). 1000 VU is well over capacity for this branch; needs Axiom server-side view to confirm whether the 60s wall is Vercel function timeout vs DB. macOS quirk: k6 v2.0.0 needed `--insecure-skip-tls-verify` (Go cert verifier failed on the Vercel cert; curl was fine) |

Notes:
- Preview deployments serve the tRPC API **same-origin** (`/api/v1/trpc`, Next.js rewrite) — so APP_URL and BASE_URL share the host.
- To distinguish a run, set `LABEL=...` when calling `run.sh`; it lands in the report filename. Keep this table updated with tier/cap + target since those are NOT in the filename.
</content>
</invoke>
